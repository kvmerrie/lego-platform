{
  "name": "commerce-sync",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/commerce-sync/src",
  "projectType": "application",
  "tags": ["scope:api", "type:app", "platform:server"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/commerce-sync",
        "format": ["cjs"],
        "bundle": true,
        "main": "apps/commerce-sync/src/main.ts",
        "tsConfig": "apps/commerce-sync/tsconfig.app.json",
        "assets": []
      },
      "configurations": {
        "development": {},
        "production": {
          "esbuildOptions": {
            "sourcemap": false
          }
        }
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/commerce-sync",
        "command": "vitest run src/lib/*.spec.ts --config ../../libs/vitest.config.mts"
      }
    },
    "run": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/commerce-sync/main.cjs"
      }
    }
  }
}
