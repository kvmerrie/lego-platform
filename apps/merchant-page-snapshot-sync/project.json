{
  "name": "merchant-page-snapshot-sync",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/merchant-page-snapshot-sync/src",
  "projectType": "application",
  "tags": ["scope:api", "type:app", "platform:server"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/merchant-page-snapshot-sync",
        "format": ["cjs"],
        "bundle": true,
        "main": "apps/merchant-page-snapshot-sync/src/main.ts",
        "tsConfig": "apps/merchant-page-snapshot-sync/tsconfig.app.json",
        "assets": []
      },
      "configurations": {
        "development": {},
        "production": {
          "esbuildOptions": {
            "sourcemap": false
          }
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "run": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/merchant-page-snapshot-sync/main.cjs"
      }
    }
  }
}
