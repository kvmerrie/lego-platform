{
  "name": "brickspoint-feed-sync",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/brickspoint-feed-sync/src",
  "projectType": "application",
  "tags": ["scope:api", "type:app", "platform:server"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/brickspoint-feed-sync",
        "format": ["cjs"],
        "bundle": true,
        "main": "apps/brickspoint-feed-sync/src/main.ts",
        "tsConfig": "apps/brickspoint-feed-sync/tsconfig.app.json",
        "skipTypeCheck": true,
        "assets": []
      },
      "configurations": {
        "development": {},
        "production": {
          "esbuildOptions": {
            "sourcemap": false
          }
        }
      }
    },
    "run": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/brickspoint-feed-sync/main.cjs"
      }
    }
  }
}
