{
  "name": "shared-config",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/config/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:config", "platform:shared"],
  "// targets": "to see all targets run: nx show project config --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/shared/config",
        "command": "vitest run src/lib/config.spec.ts --config ../../vitest.config.mts"
      }
    }
  }
}
